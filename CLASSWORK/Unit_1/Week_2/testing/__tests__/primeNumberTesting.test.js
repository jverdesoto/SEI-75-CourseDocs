const isPrime = require('../js/primeNumberTesting')

test('Test is a number is prime', () => {
expect(isPrime(127)).toBe(true)
})





const howManyPrimes = require('../js/primeNumberTesting')

test('Test is a number is prime', () => {
expect(
})