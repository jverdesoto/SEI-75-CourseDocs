const isPrime = require('../js/testThisFeature')
// const getPrimesUpTo = require('../js/testThisFeature')
// const findNextPrimes = require('../js/testThisFeature')

test('Test if a number is a prime number', () => {
    expect(isPrime(59)).toBe(true)
})
// test('Test if numbers are the next primes', () => {
//     expect(getPrimesUpTo).toBe(true)
// })
// test('Test if output gets all primes up to', () => {
//     expect(findNextPrimes).toBe(true)
// })

